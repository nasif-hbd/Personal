fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.40
pydantic>=2.7

# Optional — only needed if you use /sms/send or /calls/make:
# twilio>=9

# Dev — only needed to run tests/ (see ../tests/test_server.py):
# pytest>=8
# httpx>=0.27
